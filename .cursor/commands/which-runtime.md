Classify whether this issue is **PWA-only**, **desktop-only**, or **shared**, then propose the smallest safe fix path.

Follow `.cursor/skills/pwa-desktop-compat/SKILL.md` and `docs/PWA_DESKTOP_COMPAT.md`.

Do this:
1. Say which runtime bucket it is and why
2. Name the skill to use next (`fix-pwa` or `fix-desktop`)
3. List which folders are safe to touch
4. List the minimal smoke checks
5. Ask only if you truly cannot tell whether it fails in the browser PWA

My issue:
