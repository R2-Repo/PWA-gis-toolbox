# ITS Network Atlas — V3 deferred backlog

> **Status:** Explicitly deferred from V2. Not scheduled.

V2 implementation plan: [`docs/NETWORK_ATLAS_V2_BUILD.md`](NETWORK_ATLAS_V2_BUILD.md).

## Deferred ideas (from V2 brainstorm)

| Idea | Summary |
|------|---------|
| **24/7 monitoring (background watches)** | Persistent ping watches outside Atlas UI (tray/background runner) |
| **Project fences** | Named saved polygons with per-project watch profiles |
| **Outage notification wizard** | Emergency/planned outage email + map screenshot package |
| **Impact blast** | Downstream dark count from fiber-line topology — requires fiber GIS |

## Also deferred (lighter)

- Call mode, speakable summary, incident card, before/after ping snapshot, favorites, stale-import hard guard — see [`docs/NETWORK_ATLAS_V2.md`](NETWORK_ATLAS_V2.md) §13.

## Agents

Do not implement V3 items unless the user promotes them into an active build plan.
