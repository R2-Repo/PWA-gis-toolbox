# Optional GIS engines for the Windows Python sidecar (Phases 3–4).
# Install into the environment used by `python -m gis_sidecar`:
#   pip install -r requirements.txt
#
# Sidecar remains importable without these (stdlib GeoJSON path still works).
# For large PMTiles, also install tippecanoe on PATH when possible.
# For COG convert (`convert_to_cog`), install OSGeo4W / GDAL so
# gdal_translate, gdalinfo, and gdalwarp are on PATH.
duckdb>=1.1.0
pyogrio>=0.9.0
shapely>=2.0.0
pyarrow>=15.0.0
pmtiles>=3.0.0
mapbox-vector-tile>=2.0.0
mercantile>=1.2.1
